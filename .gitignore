# dependencies
node_modules

# testing
coverage

# production
collector/build
collector/build-local

# misc
.DS_Store
*.pem
*.p8
*.p12
private_keys/
.turbo

# debug
.pnpm-debug.log*

# local env files
.env
.env.prod
.env*.local

.tools
collector/config.yaml

/volumes

.tanstack

coverage.out

config.yml

dist
!.github/actions/everr-action/dist
!.github/actions/everr-action/dist/**
.github/actions/everr-action/build/
.github/actions/everr-action/README.generated.md

.output

# rust
target

# Terraform infra
.terraform/
*.tfstate
*.tfstate.*
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
terraform.tfvars
terraform.tfvars.json
.fallow/
.superpowers/
.worktrees/

# Local superpowers planning docs (specs/plans) — keep on disk, out of git
docs/superpowers/
.auth
.pnpm-store

.impeccable/critique
# impeccable local state (machine-specific cache + local consent)
.impeccable/hook.cache.json
.impeccable/config.local.json
