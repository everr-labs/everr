Annotation system after optimizations (jobs or tests):
- date, model type (e.g. GPT-5.4) and annotation

Add a suggestion on watching the CI when fixing a previous failing CI run
Show in-processing state for in-flight stuff. Now we show no-logs which feels like a bug.
Table sorting
Table alignment
FilterSelect should be a Combobox with autocomoplete. Runs branch should filter on click.
