-- Intentionally left as a no-op.
-- This migration was generated after 0003 but duplicated the same schema changes.
-- Keeping the file (instead of deleting) preserves migration journal consistency.
SELECT 1;
