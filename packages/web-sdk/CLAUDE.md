Keep the bundle size minimal and measure the gz size at each meanungful iteration.

Write the last size snapshot here to track it.

Current size:  7,858 B gz (core + web-vitals attribution build + native error capture; vitals attribution keys auto-derived raw from the attribution object, scalars only, verbatim camelCase, no allowlist; captureReactError lives in the ./react entry and tree-shakes out of the core; ceiling 9KB)

Must follow the oTel semconv when possible.
Custom attrs should be under everr. prefix
