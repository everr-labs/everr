---
"@everr/otel-web": patch
---

Capture XMLHttpRequest calls in network() with client spans, trace propagation, route naming, and sanitized URLs. Handle request reuse, synchronous requests, failures, cancellation, and cleanup on shutdown without adding dependencies.
