receivers:
  githubactions:
    # secret: my-secret
    cors:
      allowed_origins: ["*"]
      allowed_headers: ["*"]

processors:
  batch:

exporters:
  debug:
    verbosity: detailed

service:
  pipelines:
    traces:
      receivers: [githubactions]
      # processors: [batch]
      exporters: [debug]
