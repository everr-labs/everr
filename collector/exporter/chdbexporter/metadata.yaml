type: chdb
display_name: chDB Exporter

status:
  class: exporter
  stability:
    development: [profiles]
    alpha: [metrics]
    beta: [traces, logs]
  distributions: [contrib]
  codeowners:
    active: [hanjm, Frapschen, SpencerTorres]
    emeritus: [dmitryax]

tests:
  config:
    endpoint: clickhouse://localhost:9000
