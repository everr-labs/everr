name: Everr CLI CI

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  pull_request:
    paths:
      - .github/workflows/build-everr-cli.yml
      - everr-cli/**
      - packages/docs/Dockerfile
      - packages/docs/public/install.sh
      - packages/docs/content/docs/cli/**
  push:
    branches: [main]
    paths:
      - .github/workflows/build-everr-cli.yml
      - everr-cli/**
      - packages/docs/Dockerfile
      - packages/docs/public/install.sh
      - packages/docs/content/docs/cli/**

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: everr-cli

      - name: Test
        run: cargo test --manifest-path everr-cli/Cargo.toml

      - name: Build release binary
        run: cargo build --release --manifest-path everr-cli/Cargo.toml

      - name: Smoke test
        run: ./target/release/everr --help
